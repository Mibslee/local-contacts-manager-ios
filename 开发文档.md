# 本地通讯录管家 - 鸿蒙版开发文档

## 一、应用概述

**应用名称：** 本地通讯录管家

**核心定位：** 一款完全本地化运行的通讯录健康管理工具，专注于检测和修复通讯录中的各种问题，确保数据干净、规范、无冗余。

**核心特性：**
- 永不联网，所有数据仅存储在本地设备
- 自动检测通讯录健康问题并评分
- 支持 10 种清理优化选项
- 支持导入/导出 vCard 和 CSV 格式
- 操作前自动备份，支持一键恢复

---

## 二、技术架构

### 2.1 架构模式：MVVM

```
┌─────────────────────────────────────────────────┐
│                    Views (UI层)                    │
│  ContentView / OptimizedHomeView / ImportView     │
│  ExportView / CleanupView / SettingsView          │
├─────────────────────────────────────────────────┤
│               ViewModels (业务逻辑层)              │
│  AppViewModel / CleanupViewModel / ImportViewModel│
├─────────────────────────────────────────────────┤
│               Services (服务层)                    │
│  HealthAnalyzer / ContactNormalizer               │
│  ContactDeduplicator / ContactExporter            │
│  ContactImporter / ContactsManager                │
├─────────────────────────────────────────────────┤
│               Models (数据模型层)                   │
│  ContactItem / HealthReport / CleanupOption       │
│  CleanupResult / ExportFormat                     │
├─────────────────────────────────────────────────┤
│               Utils (工具类)                       │
│  PinyinHelper / OperationLogger                   │
└─────────────────────────────────────────────────┘
```

---

## 三、数据模型 (Models)

### 3.1 ContactItem - 联系人模型

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 联系人唯一标识 |
| familyName | String | 姓 |
| givenName | String | 名 |
| fullName | String | 全名（姓+名拼接） |
| phoneNumbers | LabeledValue[] | 手机号列表（带标签） |
| emailAddresses | LabeledValue[] | 邮箱列表（带标签） |
| organization | String | 公司/组织 |
| department | String | 部门 |
| birthday | Date? | 生日 |
| note | String | 备注 |
| imageData | ArrayBuffer? | 头像数据 |
| tags | String[] | 标签 |

**计算属性：**
- `initials`: 姓名首字母（如"张三"返回"张"首字）
- `hasPhone`: 是否有手机号
- `hasEmail`: 是否有邮箱
- `isEmpty`: 是否为空联系人（无电话无邮箱）

**LabeledValue 子模型：**
```typescript
interface LabeledValue {
  id: string;
  label: string;  // "手机"、"工作"、"邮箱" 等
  value: string;  // 实际值
}
```

### 3.2 HealthReport - 健康报告模型

| 字段 | 类型 | 说明 |
|------|------|------|
| totalContacts | number | 联系人总数 |
| totalPhoneNumbers | number | 手机号总数 |
| totalEmails | number | 邮箱总数 |
| contactsMissingName | number | 缺少姓名的联系人数 |
| contactsNameNeedsStandardize | number | 姓名需标准化数 |
| contactsInconsistentPhonePrefix | number | 手机号前缀不一致数 |
| contactsInconsistentPhoneLabel | number | 手机号标签不一致数 |
| contactsInconsistentEmailLabel | number | 邮箱标签不一致数 |
| contactsDuplicatePhone | number | 有重复手机号的联系人数 |
| contactsDuplicateEmail | number | 有重复邮箱的联系人数 |
| duplicateContactsCount | number | 重复联系人数 |
| emptyContactsCount | number | 空联系人数 |
| invalidEmailCount | number | 无效邮箱数 |
| contactsGarbledPhone | number | 手机号异常数 |

**健康分计算公式：**
```
score = 100 - penalties

penalties:
  - 重复联系人比例 × 30
  - 姓名未拆分比例 × 20
  - 手机号不一致比例 × 20
  - 邮箱不一致比例 × 15
  - 空联系人比例 × 15
```

**评分等级：**
- 80-100分：绿色（良好）
- 50-79分：橙色（一般）
- 0-49分：红色（较差）

**IssueType 枚举（11种问题类型）：**

| 类型 | 名称 | 说明 |
|------|------|------|
| nameNeedsStandardize | 姓名需标准化 | 姓名未拆分为姓/名 |
| nameNotSplit | 姓名未拆分 | 全名字段未分开存储 |
| phonePrefixInconsistent | 手机号前缀不统一 | 有的带+86，有的不带 |
| phoneLabelInconsistent | 手机标签不统一 | 有的标"手机"，有的标"iPhone" |
| phoneDuplicate | 同人号码重复 | 同一人有多个相同号码 |
| phoneGarbled | 手机号异常 | 号码格式异常或乱码 |
| emailLabelInconsistent | 邮箱标签不统一 | 有的标"工作"，有的标"个人" |
| emailDuplicate | 同人邮箱重复 | 同一人有多个相同邮箱 |
| emailInvalid | 无效邮箱 | 邮箱格式不正确 |
| contactDuplicate | 重复联系人 | 存在重复的联系人记录 |
| emptyContact | 空联系人 | 没有任何联系方式 |

### 3.3 CleanupOption - 清理选项枚举

| 选项 | 说明 |
|------|------|
| nameNormalization | 姓名标准化（自动拆分姓/名） |
| phoneClean | 手机号清理（提取纯数字） |
| phonePrefixUnify | 手机号前缀统一（移除+86/86） |
| phoneLabelUnify | 手机标签统一（统一为"手机"） |
| phoneDeduplicate | 手机号去重（同人重复号码保留一个） |
| emailLabelUnify | 邮箱标签统一（统一为"邮箱"） |
| emailDeduplicate | 邮箱去重（同人重复邮箱保留一个） |
| emailValidation | 邮箱格式校验（标记无效邮箱） |
| contactDeduplicate | 重复联系人合并 |
| removeEmptyContacts | 删除空联系人 |

### 3.4 MergeStrategy - 合并策略枚举

| 策略 | 说明 |
|------|------|
| keepMoreRecords | 保留更多记录 |
| keepMoreInfo | 信息更完整优先 |
| keepLatest | 最新记录优先 |

### 3.5 ExportFormat - 导出格式枚举

| 格式 | 扩展名 | MIME类型 |
|------|--------|----------|
| vCard | .vcf | text/vcard |
| CSV | .csv | text/csv |
| Excel兼容CSV | .csv | text/csv |

### 3.6 CleanupResult - 清理结果

| 字段 | 类型 | 说明 |
|------|------|------|
| option | CleanupOption | 清理选项 |
| beforeCount | number | 处理前数量 |
| afterCount | number | 处理后数量 |
| details | string[] | 详情描述 |
| affectedContacts | ContactItem[] | 受影响的联系人 |

### 3.7 CleanupSummary - 清理汇总

| 字段 | 类型 | 说明 |
|------|------|------|
| results | CleanupResult[] | 所有清理结果 |
| startTime | Date | 开始时间 |
| endTime | Date | 结束时间 |
| duration | number | 耗时（秒） |
| totalImproved | number | 总优化数量 |
| hasImprovements | boolean | 是否有优化 |

---

## 四、服务层 (Services)

### 4.1 HealthAnalyzer - 健康分析器

**核心方法：**
```typescript
analyze(contacts: ContactItem[]): HealthReport
```

**分析逻辑：**
1. 遍历所有联系人
2. 统计各类型问题数量
3. 使用 ContactDeduplicator 检测重复联系人
4. 生成 HealthReport

**手机号验证规则（中国号码）：**
- 11位手机号：以1开头
- 客服/服务电话：5-6位
- 固定电话：7-8位
- 带区号固定电话：10-12位
- 国际号码：5-15位

### 4.2 ContactNormalizer - 联系人标准化器

**核心方法：**

| 方法 | 功能 |
|------|------|
| `normalizeName(contact)` | 姓名标准化（拆分姓/名） |
| `normalizePhoneNumber(phone)` | 手机号标准化（统一前缀） |
| `unifyPhoneLabel(label)` | 手机标签统一 |
| `unifyEmailLabel(label)` | 邮箱标签统一 |
| `isValidEmail(email)` | 邮箱格式校验 |
| `normalizeAll(contacts, options)` | 批量标准化 |

**姓名拆分逻辑：**
1. 检查是否为复姓（欧阳、上官、司马等60+个复姓）
2. 如果是复姓，复姓为姓，剩余为名
3. 否则第一个字为姓，剩余为名

**手机号前缀处理：**
- `+86` 前缀：移除 `+86`
- `86` 前缀（超过11位）：移除 `86`
- 11位手机号（1开头）：保持不变
- 固定电话（7-12位）：保持不变

**标签统一规则：**
- 手机标签：包含"mobile"、"手机"、"mp"、"tel"、"phone" → 统一为"手机"
- 邮箱标签：包含"email"、"邮箱"、"邮件"、"e-mail"、"mail" → 统一为"邮箱"

**邮箱验证正则：**
```
[A-Z0-9a-z._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,64}
```

### 4.3 ContactDeduplicator - 联系人去重器

**DuplicateGroup 数据结构：**
```typescript
interface DuplicateGroup {
  id: string;
  contacts: ContactItem[];
  reason: string;
  isSelected: boolean;
}
```

**核心方法：**

| 方法 | 功能 |
|------|------|
| `findDuplicates(contacts)` | 查找重复联系人组 |
| `findDuplicatePhones(contacts)` | 查找跨联系人重复手机号 |
| `findDuplicateEmails(contacts)` | 查找跨联系人重复邮箱 |
| `findDuplicatePhonesInContact(contact)` | 查找单个联系人内重复手机号 |
| `findDuplicateEmailsInContact(contact)` | 查找单个联系人内重复邮箱 |
| `mergeContacts(contacts, strategy)` | 合并重复联系人 |

**重复检测逻辑：**
1. 按姓名分组（去除空格、转小写）
2. 在同名组内检查是否有相同手机号或邮箱
3. 手机号比较时移除 `86` 前缀
4. 邮箱比较时转小写

**重复原因检测：**
- "相同姓名 + 相同手机号"
- "相同姓名 + 相同邮箱"
- "相同姓名"
- "相同手机号"
- "相同邮箱"
- "疑似重复"

### 4.4 ContactExporter - 联系人导出器

**核心方法：**

| 方法 | 功能 |
|------|------|
| `exportToVCard(contacts)` | 导出为 vCard 格式 |
| `exportToCSV(contacts)` | 导出为 CSV 格式 |
| `saveToFile(content, fileName)` | 保存字符串到文件 |
| `saveDataToFile(data, fileName)` | 保存数据到文件 |

**vCard 格式示例：**
```
BEGIN:VCARD
VERSION:3.0
N:张;三;;;
FN:张三
TEL;TYPE=CELL:13800138000
EMAIL;TYPE=INTERNET:zhangsan@example.com
ORG:某某公司;技术部
END:VCARD
```

**CSV 格式：**
```
姓名,姓,名,手机1,手机1标签,手机2,手机2标签,邮箱1,邮箱1标签,邮箱2,邮箱2标签,公司,部门,备注
```

### 4.5 ContactImporter - 联系人导入器

**ImportResult 数据结构：**
```typescript
interface ImportResult {
  total: number;
  success: number;
  failed: number;
  skipped: number;
  contacts: ContactItem[];
  errors: string[];
}
```

**核心方法：**

| 方法 | 功能 |
|------|------|
| `importFromVCard(data)` | 从 vCard 数据导入 |
| `importFromCSV(data, encoding)` | 从 CSV 数据导入 |

**CSV 字段自动映射：**

| CSV表头关键词 | 映射字段 |
|--------------|----------|
| 姓名/name/联系人 | name |
| 姓/family/lastname | familyName |
| 名/given/firstname | givenName |
| 手机/电话/mobile/phone/tel | phone |
| 手机2/电话2/phone2 | phone2 |
| 邮箱/email/邮件 | email |
| 公司/组织/company/org | company |

### 4.6 ContactsManager - 系统通讯录管理器

**核心方法：**

| 方法 | 功能 |
|------|------|
| `requestAccess()` | 请求通讯录访问权限 |
| `fetchAllContacts()` | 获取所有系统联系人 |
| `saveContact(contact)` | 保存单个联系人 |
| `deleteContact(contactId)` | 删除联系人 |
| `batchSaveContacts(contacts)` | 批量保存联系人 |

---

## 五、ViewModels

### 5.1 AppViewModel - 应用主 ViewModel

**状态属性：**

| 属性 | 类型 | 说明 |
|------|------|------|
| contacts | ContactItem[] | 联系人列表 |
| healthReport | HealthReport | 健康报告 |
| isLoading | boolean | 是否加载中 |
| isAuthorized | boolean | 是否已授权 |
| errorMessage | string | 错误信息 |
| searchText | string | 搜索关键词 |
| loadingProgress | number | 加载进度 |
| loadedContactsCount | number | 已加载联系人数 |

**核心方法：**

| 方法 | 功能 |
|------|------|
| `checkAuthorization()` | 检查授权状态 |
| `requestAccess()` | 请求通讯录权限 |
| `loadContacts()` | 加载联系人（异步） |
| `refresh()` | 刷新数据 |
| `contactsForIssue(issueType)` | 获取特定问题的联系人列表 |
| `issueTitle(issueType)` | 获取问题标题 |

### 5.2 CleanupViewModel - 清理 ViewModel

**状态属性：**

| 属性 | 类型 | 说明 |
|------|------|------|
| selectedOptions | Set<CleanupOption> | 选中的清理选项 |
| mergeStrategy | MergeStrategy | 合并策略 |
| isProcessing | boolean | 是否处理中 |
| processedContacts | ContactItem[] | 处理后的联系人 |
| cleanupSummary | CleanupSummary | 清理汇总 |
| showResult | boolean | 是否显示结果 |
| duplicateGroups | DuplicateGroup[] | 重复联系人组 |
| hasBackup | boolean | 是否有备份 |
| isWritingBack | boolean | 是否正在写入 |
| backupFilePath | string | 备份文件路径 |
| writeBackProgress | number | 写入进度 |
| excludedContactIDs | Set<string> | 排除的联系人ID |

**核心方法：**

| 方法 | 功能 |
|------|------|
| `runCleanup(contacts)` | 执行清理操作 |
| `backupContacts(contacts)` | 备份联系人为 vCard |
| `writeBackToSystemDirect()` | 写入系统通讯录 |
| `restoreFromBackup()` | 从备份恢复 |

**清理执行流程：**
1. 在后台线程执行所有清理操作
2. 按选中的选项依次处理
3. 支持排除特定联系人

**写入系统流程：**
1. 先备份当前通讯录
2. 删除现有联系人
3. 添加处理后的联系人
4. 更新写入进度

### 5.3 ImportViewModel - 导入 ViewModel

**状态属性：**

| 属性 | 类型 | 说明 |
|------|------|------|
| importedContacts | ContactItem[] | 导入的联系人 |
| importResult | ImportResult | 导入结果 |
| isImporting | boolean | 是否导入中 |
| showFilePicker | boolean | 是否显示文件选择器 |
| selectedFileType | FileType | 选择的文件类型 |

**核心方法：**

| 方法 | 功能 |
|------|------|
| `importFile(data, fileName)` | 导入文件 |
| `mergeToSystem(contacts)` | 合并到系统通讯录 |

---

## 六、页面结构 (Views)

### 6.1 页面导航结构

```
ContentView (根视图)
├── Tabs
│   ├── Tab 0: OptimizedHomeView (概览/首页)
│   ├── Tab 1: ImportView (导入)
│   ├── Tab 2: ExportView (导出)
│   └── Tab 3: SettingsView (设置)
│
├── Sheet: IssueDetailView (问题详情)
├── Sheet: PreExecuteResultView (预执行结果)
├── Sheet: CleanupResultView (清理结果)
└── Sheet: ShareSheet (分享)
```

### 6.2 OptimizedHomeView - 首页

**页面组成：**
1. **权限请求视图** - 未授权时显示
2. **加载视图** - 加载中显示
3. **错误视图** - 出错时显示
4. **健康评分卡片** - 圆形进度条显示分数
5. **统计卡片** - 联系人/手机号/邮箱数量
6. **问题列表** - 可优化项目列表
7. **操作按钮** - 写入/恢复按钮

**交互功能：**
- 下拉刷新
- 搜索联系人
- 点击问题查看详情
- 预执行优化
- 忽略问题
- 写入系统通讯录
- 从备份恢复

### 6.3 IssueDetailView - 问题详情页

**功能：**
- 显示存在该问题的所有联系人
- 支持全选/取消全选
- 支持单个联系人选择
- 支持搜索过滤
- 预执行优化按钮
- 忽略此问题按钮

### 6.4 ImportView - 导入页

**功能：**
- 从 vCard 文件导入
- 从 CSV 文件导入
- 导入结果统计
- 导入联系人预览（最多显示20个）
- 合并到系统通讯录

### 6.5 ExportView - 导出页

**功能：**
- 选择导出格式（vCard/CSV/Excel兼容）
- 显示导出信息（联系人数量、格式）
- 导出并分享

### 6.6 SettingsView - 设置页

**设置项：**

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| phoneDefaultLabel | Select | "手机" | 手机号默认标签 |
| emailDefaultLabel | Select | "邮箱" | 邮箱默认标签 |
| preservePhonePrefix | Toggle | false | 保留国际区号 |
| autoBackup | Toggle | true | 操作前自动备份 |

### 6.7 CleanupView - 整理页

**功能：**
- 备份与恢复管理
- 10种清理选项选择（支持全选）
- 合并策略选择（重复联系人时显示）
- 开始整理按钮
- 整理结果展示

### 6.8 CleanupResultView - 整理结果页

**功能：**
- 结果汇总（联系人数量、耗时）
- 各项清理结果卡片
- 查看受影响联系人
- 写入系统通讯录按钮
- 导出为文件按钮

### 6.9 通用组件

**ContactRow - 联系人行组件：**
- 显示头像首字母
- 显示姓名
- 显示第一个手机号

**StatCard - 统计卡片组件：**
- 图标
- 数值
- 标题

---

## 七、工具类 (Utils)

### 7.1 PinyinHelper - 拼音助手

**核心方法：**

| 方法 | 功能 |
|------|------|
| `toPinyin(chinese)` | 中文转拼音 |
| `toPinyinInitials(chinese)` | 中文转拼音首字母 |

**实现方式：**
- 内置 500+ 常见姓氏拼音映射表
- 支持 60+ 复姓识别
- 优先匹配复姓，再匹配单字

### 7.2 OperationLogger - 操作日志器

**LogEntry 结构：**
```typescript
interface LogEntry {
  id: string;
  timestamp: Date;
  operation: string;
  details: string;
  affectedCount: number;
}
```

**核心方法：**

| 方法 | 功能 |
|------|------|
| `log(operation, details, affectedCount)` | 记录操作日志 |
| `clearLogs()` | 清空日志 |

**存储方式：**
- 使用 Preferences 存储
- 最多保留 100 条日志
- JSON 序列化

---

## 八、鸿蒙版开发要点

### 8.1 核心 API 对应关系

| iOS | 鸿蒙 | 说明 |
|-----|------|------|
| CNContactStore | @ohos.contact | 系统通讯录访问 |
| CNContact | Contact | 联系人对象 |
| CNMutableContact | Contact (可变) | 可修改的联系人 |
| CNSaveRequest | contact.save/add/delete | 保存操作 |
| CNContactVCardSerialization | 自实现 vCard 解析 | vCard 序列化 |
| SwiftUI | ArkUI | UI 框架 |
| @Published | @State + @Link | 状态管理 |
| @StateObject | @State + aboutToAppear | 状态对象 |
| @EnvironmentObject | AppStorage / 全局变量 | 环境对象 |
| Task/async/await | 异步任务 | 异步操作 |
| UserDefaults | Preferences | 本地存储 |

### 8.2 权限申请

**鸿蒙权限配置 (module.json5)：**
```json5
{
  "module": {
    "requestPermissions": [
      {
        "name": "ohos.permission.READ_CONTACTS"
      },
      {
        "name": "ohos.permission.WRITE_CONTACTS"
      }
    ]
  }
}
```

**代码中申请权限：**
```typescript
import { abilityAccessCtrl } from '@kit.AbilityKit';

let atManager = abilityAccessCtrl.createAtManager();
await atManager.requestPermissionsFromUser(context, [
  'ohos.permission.READ_CONTACTS',
  'ohos.permission.WRITE_CONTACTS'
]);
```

### 8.3 读取联系人

**鸿蒙实现：**
```typescript
import { contact } from '@kit.ContactsKit';

let contactStore = contact.getContactAbility(context);
let predicates = new contact.ContactPredicates();
let contacts = await contactStore.queryContacts(predicates);
```

### 8.4 vCard 解析器实现

**需要自行实现 vCard 解析器，参考 RFC 6350 规范：**
- 解析 `BEGIN:VCARD` / `END:VCARD`
- 解析 `N:` 字段（姓名）
- 解析 `FN:` 字段（全名）
- 解析 `TEL:` 字段（电话）
- 解析 `EMAIL:` 字段（邮箱）
- 解析 `ORG:` 字段（组织）

**vCard 解析核心逻辑：**
```typescript
function parseVCard(data: string): ContactItem[] {
  const contacts: ContactItem[] = [];
  const blocks = data.split('BEGIN:VCARD');

  for (const block of blocks) {
    if (!block.trim()) continue;

    const contact: ContactItem = {
      id: generateId(),
      familyName: '',
      givenName: '',
      fullName: '',
      phoneNumbers: [],
      emailAddresses: [],
      organization: '',
      department: '',
      note: '',
      tags: []
    };

    const lines = block.split('\n');
    for (const line of lines) {
      if (line.startsWith('N:')) {
        const parts = line.substring(2).split(';');
        contact.familyName = parts[0] || '';
        contact.givenName = parts[1] || '';
      } else if (line.startsWith('FN:')) {
        contact.fullName = line.substring(3);
      } else if (line.startsWith('TEL')) {
        const match = line.match(/TEL;TYPE=(\w+):(.+)/);
        if (match) {
          contact.phoneNumbers.push({
            id: generateId(),
            label: match[1],
            value: match[2]
          });
        }
      } else if (line.startsWith('EMAIL')) {
        const match = line.match(/EMAIL;TYPE=(\w+):(.+)/);
        if (match) {
          contact.emailAddresses.push({
            id: generateId(),
            label: match[1],
            value: match[2]
          });
        }
      } else if (line.startsWith('ORG:')) {
        const parts = line.substring(4).split(';');
        contact.organization = parts[0] || '';
        contact.department = parts[1] || '';
      }
    }

    if (contact.fullName || contact.phoneNumbers.length > 0) {
      contacts.push(contact);
    }
  }

  return contacts;
}
```

### 8.5 CSV 解析器实现

**通用逻辑（可直接移植）：**
```typescript
function parseCSVLine(line: string): string[] {
  const result: string[] = [];
  let current = '';
  let inQuotes = false;

  for (let i = 0; i < line.length; i++) {
    const char = line[i];
    if (char === '"') {
      if (inQuotes && line[i + 1] === '"') {
        current += '"';
        i++;
      } else {
        inQuotes = !inQuotes;
      }
    } else if (char === ',' && !inQuotes) {
      result.push(current);
      current = '';
    } else {
      current += char;
    }
  }
  result.push(current);

  return result;
}
```

### 8.6 UI 框架对应

| iOS SwiftUI | 鸿蒙 ArkUI |
|-------------|-----------|
| VStack | Column |
| HStack | Row |
| List | List |
| NavigationStack | Navigation |
| TabView | Tabs + TabContent |
| Button | Button |
| Text | Text |
| Image | Image |
| TextField | TextInput |
| Toggle | Toggle |
| Picker | Select |
| ProgressView | Progress |
| Circle | Circle |
| RoundedRectangle | RoundedRect |
| .padding() | .padding() |
| .background() | .backgroundColor() |
| .clipShape() | .clip() |
| .font() | .fontSize() .fontWeight() |
| .foregroundStyle() | .fontColor() |

### 8.7 ArkUI 首页实现示例

```typescript
@Entry
@Component
struct OptimizedHomeView {
  @State contacts: ContactItem[] = [];
  @State healthReport: HealthReport = HealthReport.empty;
  @State isLoading: boolean = false;
  @State isAuthorized: boolean = false;
  @State searchText: string = '';

  build() {
    Navigation() {
      Scroll() {
        Column({ space: 20 }) {
          if (!this.isAuthorized) {
            this.permissionView()
          } else if (this.isLoading) {
            this.loadingView()
          } else {
            this.healthScoreCard()
            this.statsSection()
            this.issuesSection()
            this.actionSection()
          }
        }
        .padding(16)
      }
      .refreshable(async () => {
        await this.refresh()
      })
    }
    .title('通讯录管家')
  }

  @Builder
  healthScoreCard() {
    Column({ space: 12 }) {
      Stack() {
        Circle({ width: 120, height: 120 })
          .stroke(Color.Gray, { width: 12 })

        Progress({ value: this.healthReport.score, total: 100 })
          .style(ProgressStyle.Ring)
          .color(this.scoreColor)

        Column({ space: 2 }) {
          Text(`${this.healthReport.score}`)
            .fontSize(36)
            .fontWeight(FontWeight.Bold)
            .fontColor(this.scoreColor)
          Text('健康分')
            .fontSize(12)
            .fontColor(Color.Gray)
        }
      }

      Text(`${this.healthReport.totalContacts} 位联系人`)
        .fontSize(16)
        .fontWeight(FontWeight.Bold)
    }
    .width('100%')
    .padding({ top: 24, bottom: 24 })
    .backgroundColor('#F5F5F5')
    .borderRadius(16)
  }

  @Builder
  statsSection() {
    Row({ space: 12 }) {
      this.statCard('person.2.fill', `${this.healthReport.totalContacts}`, '联系人', '#007AFF')
      this.statCard('phone.fill', `${this.healthReport.totalPhoneNumbers}`, '手机号', '#34C759')
      this.statCard('envelope.fill', `${this.healthReport.totalEmails}`, '邮箱', '#FF9500')
    }
  }

  @Builder
  statCard(icon: string, value: string, title: string, color: Color) {
    Column({ space: 8 }) {
      Image($r(`app.media.${icon}`))
        .width(24)
        .height(24)
        .fillColor(color)
      Text(value)
        .fontSize(20)
        .fontWeight(FontWeight.Bold)
      Text(title)
        .fontSize(12)
        .fontColor(Color.Gray)
    }
    .layoutWeight(1)
    .padding({ top: 16, bottom: 16 })
    .backgroundColor('#F5F5F5')
    .borderRadius(12)
  }

  get scoreColor(): Color {
    const score = this.healthReport.score;
    if (score >= 80) return Color.Green;
    if (score >= 50) return '#FF9500';
    return Color.Red;
  }
}
```

### 8.8 数据存储

**设置存储：**
- iOS: @AppStorage / UserDefaults
- 鸿蒙: Preferences (AppStorage)

**操作日志存储：**
- iOS: UserDefaults + JSON
- 鸿蒙: Preferences 或关系型数据库

**备份文件存储：**
- iOS: FileManager.temporaryDirectory
- 鸿蒙: 应用沙箱临时目录

**鸿蒙 Preferences 使用示例：**
```typescript
import { preferences } from '@kit.ArkData';

let context = getContext(this);
let dataPreferences = await preferences.getPreferences(context, 'settings');

// 写入
await dataPreferences.put('phoneDefaultLabel', '手机');
await dataPreferences.flush();

// 读取
let value = await dataPreferences.get('phoneDefaultLabel', '手机');
```

### 8.9 异步操作处理

**鸿蒙 TaskPool 使用示例：**
```typescript
import { taskPool } from '@kit.ArkTS';

async function loadContacts() {
  this.isLoading = true;

  // 在 TaskPool 中执行耗时操作
  let result = await taskPool.execute(async () => {
    // 读取联系人
    return contacts;
  });

  this.contacts = result;
  this.isLoading = false;
}
```

### 8.10 分享功能

**鸿蒙分享实现：**
```typescript
import { share } from '@kit.ShareKit';

let shareData: share.ShareData = {
  utd: 'general.file',
  content: fileUri,
  title: '导出文件'
};
await share.share(context, shareData);
```

---

## 九、复姓列表（完整）

以下是应用中支持的复姓，共 60+ 个：

```
欧阳、太史、端木、上官、司马、东方、独孤、南宫、万俟、闻人、
夏侯、诸葛、尉迟、公羊、赫连、澹台、皇甫、宗政、濮阳、公冶、
太叔、申屠、公孙、慕容、仲孙、钟离、长孙、宇文、司徒、鲜于、
司空、令狐、百里、东郭、南门、呼延、羊舌、微生、公户、公玉、
公仪、梁丘、公仲、公上、公门、公山、公坚、公伯、左丘、公祖、
亓官、司寇、颛孙、子车、壤驷、公良、夹谷、宰父、谷梁、段干、
拓跋、乐正、漆雕、公西、巫马、公乘、公冶、宗政
```

---

## 十、应用流程图

### 10.1 主流程

```
启动应用
  ↓
检查通讯录权限
  ↓
[未授权] → 显示授权页面 → 用户授权 → 加载联系人
[已授权] → 直接加载联系人
  ↓
加载联系人（显示进度）
  ↓
执行健康分析
  ↓
显示首页（健康评分 + 问题列表）
  ↓
用户操作：
  ├─ 点击问题 → 查看详情 → 预执行优化/忽略
  ├─ 写入系统通讯录 → 备份 → 删除旧 → 添加新
  ├─ 导入 → 选择文件 → 解析 → 合并到系统
  ├─ 导出 → 选择格式 → 生成文件 → 分享
  └─ 设置 → 修改默认配置
```

### 10.2 清理流程

```
选择清理选项
  ↓
点击"开始整理"
  ↓
备份当前通讯录（vCard格式）
  ↓
后台线程执行清理：
  ├─ 姓名标准化
  ├─ 手机号清理
  ├─ 手机号前缀统一
  ├─ 手机标签统一
  ├─ 邮箱标签统一
  ├─ 手机号去重
  ├─ 邮箱去重
  ├─ 邮箱格式校验
  ├─ 重复联系人合并
  └─ 删除空联系人
  ↓
生成清理结果
  ↓
用户确认写入系统通讯录
  ↓
删除所有旧联系人
  ↓
添加新联系人
  ↓
完成
```

---

## 十一、文件结构

```
本地通讯录管家/
├── models/
│   ├── ContactItem.ets          # 联系人数据模型
│   ├── HealthReport.ets         # 健康报告模型
│   ├── CleanupOption.ets        # 清理选项枚举
│   ├── CleanupResult.ets        # 清理结果模型
│   └── ExportFormat.ets         # 导出格式枚举
│
├── viewmodels/
│   ├── AppViewModel.ets         # 应用主ViewModel
│   ├── CleanupViewModel.ets     # 清理ViewModel
│   └── ImportViewModel.ets      # 导入ViewModel
│
├── services/
│   ├── HealthAnalyzer.ets       # 健康分析器
│   ├── ContactNormalizer.ets    # 联系人标准化器
│   ├── ContactDeduplicator.ets  # 联系人去重器
│   ├── ContactExporter.ets      # 联系人导出器
│   ├── ContactImporter.ets      # 联系人导入器
│   └── ContactsManager.ets      # 系统通讯录管理器
│   ├── VCardParser.ets          # vCard解析器
│   └── CSVParser.ets            # CSV解析器
│
├── views/
│   ├── Home/
│   │   └── OptimizedHomeView.ets # 首页
│   ├── Import/
│   │   └── ImportView.ets        # 导入页
│   ├── Export/
│   │   └── ExportView.ets        # 导出页
│   ├── Cleanup/
│   │   ├── CleanupView.ets       # 整理页
│   │   └── CleanupResultView.ets # 整理结果页
│   ├── Settings/
│   │   └── SettingsView.ets      # 设置页
│   ├── Components/
│   │   ├── ContactRow.ets        # 联系人行组件
│   │   └── IssueContactsView.ets # 问题联系人视图
│   │   └── StatCard.ets          # 统计卡片组件
│   └── ContentView.ets           # 根视图（Tabs）
│
├── utils/
│   ├── PinyinHelper.ets          # 拼音助手
│   └── OperationLogger.ets       # 操作日志器
│
├── resources/
│   └── base/
│       └── media/                # 图标资源
│
└── module.json5                  # 模块配置
```

---

## 十二、鸿蒙版新增文件

### 12.1 VCardParser.ets - vCard 解析器

**需要实现的功能：**
- 解析 vCard 格式字符串
- 转换为 ContactItem 数组
- 生成 vCard 格式字符串

### 12.2 CSVParser.ets - CSV 解析器

**需要实现的功能：**
- 解析 CSV 格式字符串
- 自动映射表头字段
- 转换为 ContactItem 数组
- 生成 CSV 格式字符串

---

## 十三、测试数据说明

导入与整理功能可使用自备的 vCard（.vcf）或 CSV 文件在真机/模拟器上验证。

**测试覆盖场景：**
- 正常联系人
- 缺少姓名的联系人
- 姓名未拆分的联系人
- 手机号前缀不一致
- 手机标签不一致
- 重复手机号
- 重复邮箱
- 无效邮箱
- 重复联系人
- 空联系人

---

## 十四、隐私与安全

**核心原则：**
- 永不联网
- 所有数据仅存储在本地设备
- 操作前自动备份
- 支持一键恢复

**鸿蒙权限声明：**
- `ohos.permission.READ_CONTACTS`: 读取通讯录
- `ohos.permission.WRITE_CONTACTS`: 写入通讯录

---

## 十五、版本信息

- **当前版本：** 1.0.0
- **iOS 最低版本：** iOS 16.0
- **鸿蒙最低版本：** API 10 (HarmonyOS 4.0)
- **开发语言：** Swift (iOS) / ArkTS (鸿蒙)
- **UI 框架：** SwiftUI (iOS) / ArkUI (鸿蒙)
